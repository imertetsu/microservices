server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/imertetsu/microservices
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: secret